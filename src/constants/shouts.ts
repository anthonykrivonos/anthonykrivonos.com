/**
 * Shouts are events that can be subscribed to from anywhere and called from anywhere. Used in cases when passing function down components is extra difficult.
 */
export const shouts = {
    /** Turn off page scrolling. */
    PAGE_SCROLL_OFF: "PAGE_SCROLL_OFF",
    /** Turn on page scrolling. */
    PAGE_SCROLL_ON: "PAGE_SCROLL_ON",
}